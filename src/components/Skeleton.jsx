/* عنصر هيكلي (Skeleton) بلمعة shimmer — يُعرض ريثما تصل البيانات من الـ API.
   يُنسّق عبر الصنف .skeleton في index.css. */
export default function Skeleton({ className = '', style }) {
  return <div className={`skeleton ${className}`} style={style} />
}
