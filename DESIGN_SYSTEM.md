# نظام تصميم جمعية البرهان

المصدر الموحّد لكل القرارات البصرية في الموقع. القيم البرمجية في [`src/theme.js`](src/theme.js) — استند إليها في كل قسم/مكوّن جديد.

> **قاعدتان للنصوص:**
> 1. **معتمة 100%** — لا شفافية (`rgba(...,a<1)`) على النص أبداً.
> 2. **أبيض افتراضياً، والتفتيح بالوزن لا باللون** — كل النصوص `#ffffff`. لجعل النص أخفت قلّل **وزن الخط**: `300` (خفيف) للثانوي، `200` (خفيف جداً) للأخفت، و`500–600` للتأكيد/العناوين. لا تُدخل ألواناً رمادية/بنفسجية باهتة.
>
> **استثناءات (ألوان تمييز مقصودة):** النحاسي `#ef9122` للروابط والأزرار، الذهبي `#d8c8a4` للوسوم.

---

## 1. الألوان

### النحاسي (التمييز / الدعوة للفعل)
| الرمز | القيمة | الاستخدام |
|------|--------|-----------|
| `copper` | `#ef9122` | أزرار CTA، روابط "قراءة المزيد" |
| `copperHover` | `#f4a63f` | حالة المرور |
| `copperDeep` | `#c9760f` | نهاية تدرّج الأزرار |
| `gold` | `#d8c8a4` | وسوم التصنيف |

### البنفسجي (الخلفيات والتوهّجات)
`purple100 #2fa7cc` ← `purple300 #1a7fa1` ← `purple500 #14607f` ← `purple700 #0e4156` ← `purple900 #071f2b` · `purpleAccent #12719a`

### النصوص (كلها صلبة ومعتمة 100%)
| الرمز | القيمة | الاستخدام |
|------|--------|-----------|
| `textPrimary` | `#ffffff` | العناوين والنصوص الأساسية |
| `textLead` | `#d2e9f2` | فقرة الهيرو التمهيدية |
| `textSoft` | `#a2becf` | نص ثانوي ليّن |
| `textMuted` | `#8fa6b0` | تسميات overline داخل البطاقات |
| `textBody` | `#9888b0` | أوصاف الفقرات |
| `textNav` | `#bcd9e6` | روابط شريط التنقّل |

---

## 2. الخطوط (التايبوغرافي)

> نظام التايبوغرافي الكامل في [`src/typography.js`](src/typography.js) — استورد `text` واستخدم النمط الجاهز مباشرةً (`text.sectionTitle`, `text.body` ...).

**العائلة:** `IBM Plex Sans Arabic` (محمّلة من Google Fonts).
**الأوزان المحمّلة:** 400 / 500 / 600 / 700 — لا تستخدم وزناً خارجها.

### سُلّم الأحجام والأنماط الموحّد
| النمط | الحجم (px) | الوزن | السطر | تباعد الأحرف | اللون |
|------|-----------|------|------|-------------|-------|
| `eyebrow` (الصغير فوق عنوان القسم) | 18 | 500 | — | 0.20em | أبيض |
| `sectionTitle` (عنوان القسم h2) | 24 | 600 | 1.3 | — | أبيض |
| `lead` (فقرة الهيرو) | 15 → 17 | **300** | 1.7 | — | أبيض |
| `cardTitle` (كل عناوين البطاقات h3) | 18 | 600 | 1.4 | — | أبيض |
| `label` (الصغير فوق عنوان البطاقة) | 12 | **300** | 1.4 | 0.12em | أبيض |
| `badge` (وسم تصنيف) | 12 | 500 | 1 | — | ذهبي (تمييز) |
| `body` (فقرات البطاقات) | 14 | **300** | 1.7 | — | أبيض |
| `action` (كل الأزرار والروابط) | 14 | 500 | — | — | أبيض / نحاسي |

الأوزان: تأكيد/عناوين `600` · تسميات/أزرار `500` · افتراضي `400` · خفيف (ثانوي) `300` · خفيف جداً `200`.

**القاعدة الموحّدة:** عنوان القسم 24 · فوقه 18 · عنوان البطاقة 18 · فوقه 12 · فقرات البطاقات 14 · الأزرار 14.

---

## 3. الزجاج (الأسطح)

النمط الموحّد عبر `glass()` في `theme.js`:
- خلفية: `linear-gradient(145deg, rgba(255,255,255,0.08), rgba(255,255,255,0.03))`
- حدّ: `0.5px solid rgba(255,255,255,0.12)` — وحدّ علوي أفتح `0.20`
- تمويه خلفي: `blur(20px)`
- **بلا ظلال صندوقية** — الاعتماد على الحدّ الرفيع فقط.

---

## 4. الزوايا والتمويه
`radius`: صغير 14px · متوسط (بطاقات) 18px · pill 999px.  
`blur` الزجاج: 20px.

---

## 5. الحركة
- **المنحنى القياسي:** `cubic-bezier(0.22, 1, 0.36, 1)`
- **ظهور العناصر** (`fade`): من `opacity:0, y:24` خلال 0.6s مع تأخير تتابعي.
- **رفع البطاقة** (`hoverLift`): `y:-4` خلال 0.25s.
- **انتقال الأقسام:** 0.6s · **تحوّل الخلفية:** 1.2s.
- **توهّج البطاقات (تنفّس):** 6s · **أضواء الخلفية الدوّارة:** 24s خطّي.

---

## 6. أنماط متكرّرة
- **التنقّل:** سكرول ذكي — تمريرة واحدة تنقل قسماً كاملاً (قفل 1.2s). الحاوية `h-screen overflow-hidden`.
- **السكرول الأفقي للبطاقات:** تلاشٍ متدرّج على الطرفين يختفي عند بلوغ الحافة + أزرار أسهم.
- **اقتطاع النص:** سطران مبرّران (`text-align: justify`) مع رابط "قراءة المزيد" ملتصق بنهاية السطر الثاني (حيلة float spacer + `clear`).
